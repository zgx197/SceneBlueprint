{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the SceneBlueprint desktop shell.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
