# SceneBlueprint 开发者测试指南

> 完整框架说明参见 `SceneBlueprint测试框架使用指南.md`

## 快速开始

### 运行测试
```
Unity 菜单 → SceneBlueprint → Tests → 🚀 运行测试 → 选择「自动运行」
```
测试完成后自动生成报告，可一键复制给 AI 分析。

### 编写新测试

```csharp
#nullable enable
using NUnit.Framework;
using SceneBlueprint.Core;
using SceneBlueprint.Tests.Utils;

namespace SceneBlueprint.Tests.Unit.Core
{
    public class MyClassTests
    {
        [Test]
        public void Method_Scenario_Expected()
        {
            // Arrange
            var bag = TestDataBuilder.CreateEmptyPropertyBag();

            // Act
            bag.Set("key", "value");

            // Assert
            bag.ShouldContainKey("key");
            bag.ShouldContain("key", "value");
        }
    }
}
```

## 测试覆盖原则

### 必须测试的场景
- 正常流程 (Happy Path)
- 边界条件 (空值、极值、临界值)
- 异常情况 (无效输入、错误状态)

### 推荐做法
- 使用 `TestDataBuilder` 创建数据，避免硬编码
- 每个测试独立创建数据，不共享可变状态
- 断言附带清晰的错误消息

### 避免做法
- 测试间共享可变数据
- 不带消息的 `Assert.IsTrue(result)`
- 硬编码测试数据

## 开发工作流

1. **编写失败测试** → TDD
2. **实现最小功能** → 让测试通过
3. **重构优化** → 保持测试通过
4. **运行全量测试** → 确认无回归

## 常见问题

**测试找不到？** → 检查 `[Test]` 属性、命名空间、asmdef 引用

**报告显示 0 个测试？** → 检查 `TestConfiguration` 的 `allowedAssemblies`

**只运行单个测试？** → 使用「手动运行」模式打开 Test Runner
