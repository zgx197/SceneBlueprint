# C7 验收记录（验收与清理）

> 日期：2026-02-14  
> 阶段：方案 C / C7（验收与清理）
> doc_status: archived  
> last_reviewed: 2026-02-15

---

## 1. 目标

- 统一文档，避免“文档漂移”。
- 清理旧入口，主路径切换到工作台。
- 入口唯一化，避免双入口造成维护与体验分裂。

---

## 2. 本阶段代码变更清单

1. `SceneBlueprintWindow` 工具栏：
   - 移除“旧入口”开关按钮与“旧配置”按钮。
2. 旧配置窗口：
   - 删除 `ConfigurationWindow` 及其 `SceneBlueprint/配置管理` 菜单入口。
3. 兼容占位清理：
   - 删除 `SceneBlueprintCompatibility` 空壳类型。
4. `SceneBlueprint_Generalized_Platform_Architecture_PlanC.md`：
   - C7 描述调整为“主入口唯一，旧入口移除”。
5. 本文档：
   - 同步更新验收项，去除兼容开关描述。

---

## 3. C1~C6 回归验收清单（执行后勾选）

- [ ] C1：打开窗口 + 创建节点 + 保存加载 + 基础 JSON 导出。
- [ ] C2：StableObjectId 生效（V2 统一语义）。
- [ ] C3：3D 标记创建/绑定流程无回归。
- [ ] C4：2D/3D 模式切换与导出适配正常。
- [ ] C5：多子图同名 bindingKey 不冲突（scoped key 正确）。
- [ ] C6：单窗口闭环、问题中心三类定位、关系面板引用展示正常。

---

## 4. C7 核心验收清单

- [ ] 工作台主路径：
  - 默认进入 `SceneBlueprint/蓝图编辑器` 完成主流程。
- [ ] 旧入口移除：
  - 菜单中不再出现 `SceneBlueprint/配置管理`。
  - 工具栏不再出现“旧入口/旧配置”相关控件。
- [ ] 文档一致性：
  - PlanC 与本验收文档均体现“主入口唯一”的策略。

---

## 5. 结论

- [ ] 本阶段通过（全部清单勾选后填写）。
- [ ] 若未通过，请记录阻塞项与回滚策略。

