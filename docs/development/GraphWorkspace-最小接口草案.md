# Graph Workspace 最小接口草案

这份文档已不再单独维护。

`Graph Workspace` 的接口边界、阶段进度、迁移判断与当前收口状态，已经统一合并到：

- `docs/development/SceneBlueprint-实现与迭代文档.md`

当前固定判断：

- `P6` 已严格 `4/4` 完成。
- 旧 `com.zgx197.nodegraph` 的核心框架资产已完成迁移与重做。
- 剩余未迁项当前不再追求 `1:1` 继续迁移。

保留这个文件只为了兼容旧链接；后续不要再把正文写回这里。
